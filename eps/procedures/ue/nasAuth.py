from eps.messages.nasAuth import authenticationResponse

class nasAuthenticationResponseProcedureHandler(object):
    def __init__(self):
#         init
        pass
    def execute(self):
#        execute 
        pass
    def sendAuthenticationResponseMessage(self):
#         creation of Authentication Request Message
        def handleIncomingAuthenticationRequestMessage():
#         recieve incoming request message and parse incoming parameters and send them to computeRes()
            return 1;
        def computeRES():
            #create parameters for the message
            #and compute res from the request parameters(AUTN,RAND, ... )
            params = "message"
            return params;
#         send message using parameters created by createParameters()
